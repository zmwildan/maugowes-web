import Styled from "styled-components"

export default Styled.form`
  .form-child {
    margin-bottom: 50px;
    label {
      display: block;
      font-weight: bold;
      margin-bottom: 10px;
      text-transform: capitalize;
    }
  }
`